# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Excel work files dropped in the project root by the team
# (invoices, packing lists, sample sheets — never commit these,
# they may contain customer info, pricing, supplier data)
*.xlsx
*.xls
样本_*

# CRM 部署文件夹 — 独立的 xmchic-crm 仓库，不属于 new-website
crm-deploy/
