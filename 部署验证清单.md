# 部署验证清单 — custom-woodenbox.com

每次 `git push` 后照此勾选。从省力到进阶排，平时跑「第一步 + 第二步 PageSpeed」即可覆盖关键点。

---

## 第一步 · 确认 Coolify 部署成功

- [ ] Coolify 面板里最新一次部署状态为 **Success（绿色）**，非 Failed。
- [ ] 构建日志（Build Logs）拉到底，能看到 `Compiled successfully` 与 `✓ Generating static pages`，无红色报错。
- [ ] 日志中**没有** `sharp` / `next/image` 相关报错（本项目依赖 sharp 做图片优化）。
- [ ] 若失败：复制报错那几行，找 Claude 排查。

---

## 第二步 · 在线工具验证（无需技术背景，最推荐）

### ① 性能 / LCP / INP（顺便补全报告第 15 项的 INP 实测值）
- [ ] 打开 https://pagespeed.web.dev → 输入 `https://www.custom-woodenbox.com/en` → Analyze。
- [ ] 看 **Mobile（移动设备）** 的 LCP / INP / CLS，记录数值（绿色为佳）。
- [ ] 把 INP 数字回填到 `SEO-AUDIT-2026-05.md` 第 15 项。

### ② FAQ 结构化数据（验证 ⑤）
- [ ] 打开 https://search.google.com/test/rich-results → 输入 `https://www.custom-woodenbox.com/en/contact` → 测试。
- [ ] 结果识别出 **FAQPage / 常见问题解答**，且无错误。

### ③ 社交分享卡（验证 ③）
- [ ] 打开 https://www.opengraph.xyz → 输入 `https://www.custom-woodenbox.com/en/products`。
- [ ] 预览是该页**专属品牌卡**（"Custom Wooden Boxes, Built to Spec"），非全站默认卡。
- [ ] 可顺带抽查 `/en/about`、`/en/contact` 等页，各自卡片不同。

---

## 第三步 · 浏览器开发者工具验证（可选 / 进阶）

Chrome 打开网站 → 按 `F12` → **Network（网络）** 标签 → 刷新页面：

- [ ] **缓存头（验证 ⑥）**：点一张产品图（`.webp`）→ Headers 里 `cache-control` 显示 `public, max-age=604800, stale-while-revalidate=86400`。
- [ ] **图片优化 / AVIF（验证 ④）**：产品页图片请求地址形如 `/_next/image?url=...`；Type 列可能显示 `avif`。
- [ ] **可访问名（验证 ⑦）**：切到 **Elements** → 选中页脚邮箱输入框 → 确认含 `aria-label="..."`。
- [ ] **重定向单跳**：地址栏输 `http://custom-woodenbox.com`（不带 www）→ 应一步 301 跳到 `https://www.custom-woodenbox.com`，无多重跳转。

---

## 本批次改动对应关系（便于定位）

| 改动 | 验证位置 |
|------|----------|
| ① Clarity 降级 lazyOnload | 第二步① INP 不应变差 |
| ② LCP 视口预加载 | 第二步① LCP；进阶看 Network 预加载 |
| ③ 各页专属 OG 图 | 第二步③ |
| ④ next/image + AVIF + sharp | 第一步日志 + 第三步图片优化 |
| ⑤ 联系页 FAQPage | 第二步② |
| ⑥ 缓存头 + minimumCacheTTL | 第三步缓存头 |
| ⑦ 表单/搜索框 a11y | 第三步可访问名 |
