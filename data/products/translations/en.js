// English baseline — empty by design. The English strings live in the per-category
// data files (data/products/{slug}.js) and serve as the fallback for all locales.
const TRANSLATIONS = {};
export default TRANSLATIONS;
