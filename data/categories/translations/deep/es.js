// Spanish deep overlays (seoTitle / seoDescription / deepContent) — pending.
// Shape: { [slug]: { seoTitle, seoDescription, deepContent: { eyebrow, title, blocks } } }
// Keep img `src` and [text](/path) link paths identical to the English source;
// translate only visible text / captions / link labels.
export default {};
